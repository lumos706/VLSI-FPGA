{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make",
            "type": "shell",
            "command": "make",
            "args": [
                "all"
            ],
            "options": {
                "cwd": "${cwd}"
            }
        },
        {
            "label": "build",
            "dependsOn": ["make"]
        }
    ]
}
